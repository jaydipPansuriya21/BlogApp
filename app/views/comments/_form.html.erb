<%= form_with model: [ @article, @article.comments.build ] do |form| %>
  
  <div class="w-auto p-3">
    <%= form.text_area :body ,class: "w-auto p-3" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>